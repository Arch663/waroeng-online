VITE_DATABASE_API_URL=http://localhost:3000
PORT=3000
CORS_ORIGIN=http://localhost:5173

# Vercel Postgres (recommended for Vercel deploy)
# Usually auto-provided by Vercel Postgres integration.
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
DATABASE_URL=

# Optional: Supabase project API vars (not used by pg.Pool)
SUPABASE_URL=
SUPABASE_KEY=

# Optional: direct Supabase Postgres URL (fallback)
SUPABASE_DB_URL=

# Pool / SSL tuning (optional)
PGSSLMODE=require
PGPOOL_MAX=20
PG_IDLE_TIMEOUT_MS=30000
PG_CONN_TIMEOUT_MS=10000

PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=3211
PGDATABASE=waroeng
