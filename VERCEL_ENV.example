# Client
VITE_DATABASE_API_URL=https://your-vercel-app.vercel.app

# Server
PORT=3000
CORS_ORIGIN=https://your-frontend-domain.vercel.app
JWT_SECRET=replace_with_strong_secret
NODE_ENV=production

# Supabase API (optional for future SDK usage)
SUPABASE_URL=https://bieszllzfozgfbdvpqkh.supabase.co
SUPABASE_KEY=sb_publishable_v5TrQqy0QZIvI_WLrH9sWg_Dr-SKejQ

# REQUIRED for existing SQL code via pg.Pool
# Get from Supabase > Project Settings > Database > Connection string (URI)
# Recommended: use Supabase pooler host (port 6543) for serverless.
SUPABASE_DB_URL=postgresql://postgres.<project-ref>:<db-password>@aws-0-<region>.pooler.supabase.com:6543/postgres

# Optional fallbacks (can be left empty if SUPABASE_DB_URL is set)
DATABASE_URL=
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# Pool/SSL tuning
PGSSLMODE=require
PGPOOL_MAX=20
PG_IDLE_TIMEOUT_MS=30000
PG_CONN_TIMEOUT_MS=10000
